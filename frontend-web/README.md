# Vee Web

Responsive web client for the existing Vee backend API.

## Setup

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env.local` when you need a different backend:

```bash
NEXT_PUBLIC_API_URL=https://your-backend.example.com/api
```

The app uses the same Laravel API endpoints as the Expo mobile app.
